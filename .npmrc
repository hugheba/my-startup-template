# Intentionally empty.
#
# pnpm 11 only reads auth and registry credentials from .npmrc. Every other
# setting is ignored here — silently, with no error — so putting one back in
# this file would look like it worked and quietly do nothing.
#
# All pnpm settings live in pnpm-workspace.yaml.
