# GENERATED by scripts/generate-dashboard.mjs — do not edit the Services group.
#
# Regenerate with `pnpm dashboard:sync`. Tiles come from forwardPorts and
# portsAttributes in .devcontainer/devcontainer.json, so a dev server in ANY language appears
# just by being forwarded and labelled. Set `protocol` to make a tile
# clickable and monitored; leave it off for non-HTTP services like Postgres.
# Everything after the Services group is copied verbatim from .devcontainer/homepage/services.extra.yaml.

- Services:
    - Next.js dev server:
        href: http://localhost:3000
        siteMonitor: http://app:3000
        description: port 3000 — process in the workspace container
    - PostgreSQL:
        description: port 5432 — container `db`
    - Adminer (database UI):
        href: http://localhost:8080
        siteMonitor: http://adminer:8080
        description: port 8080 — container `adminer`

# Hand-authored groups, appended verbatim by scripts/generate-dashboard.mjs.
# The generator owns only the `Services` group above, so nothing here is ever
# clobbered by a regeneration.
#
# Use this for things that are not a forwarded port: connection strings, links
# to runbooks, anything with no health check to speak of.
- Reference:
    - Database connection:
        description: postgresql://postgres:postgres@db:5432/postgres
    - Adminer login:
        description: server `db` · user `postgres` · password `postgres`
