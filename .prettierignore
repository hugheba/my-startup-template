node_modules
.next
.turbo
.vercel
dist
coverage
pnpm-lock.yaml
uv.lock
*.tsbuildinfo
.husky
scripts/.venv
scripts/__pycache__
scripts/.ruff_cache
scripts/.pytest_cache

# BMAD-generated: agents, workflows and their output. Generated by
# `pnpm bmad:init` and by the BMAD agents themselves — formatting them here
# would fight the generator on every run.
_bmad
_bmad-output

# Rewritten by Next.js on every build — formatting it means it shows as
# modified after each `next build`.
apps/web/next-env.d.ts

# Vendored skill documentation, not authored here (markdownlint skips it for
# the same reason).
docs/superpowers

# Generated by the CodeGuard tool, which owns their formatting (no blank
# line after headings). Prettier disagreed and rewrote them on every
# save/commit, causing perpetual churn. Let the generator be the source
# of truth.
.github/instructions/
